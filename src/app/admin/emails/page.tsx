import EmailCenter from "@/components/admin/emails/EmailCenter";

export const metadata = {
  title: "Email Center",
  description: "Manage CelebrityPass fan emails",
};

export default function EmailsPage() {
  return <EmailCenter />;
}